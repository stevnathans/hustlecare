generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  // directUrl = env("DIRECT_URL") // For migrations
}

model User {
  id                  String      @id @default(uuid())
  name                String
  email               String      @unique
  phone               String?
  password            String?
  emailVerified       DateTime?
  image               String?
  role                String      @default("user") // user, author, editor, reviewer, admin
  
  // Notification preferences
  emailNotifications  Boolean     @default(true)
  pushNotifications   Boolean     @default(false)
  marketingEmails     Boolean     @default(true)
  
  // Admin tracking
  lastLoginAt         DateTime?
  isActive            Boolean     @default(true)
  
  // Community features
  isVerified          Boolean     @default(false) // For verified badge
  verifiedAt          DateTime?
  
  carts               Cart[]
  businesses          Business[]
  comments            Comment[]
  reviews             Review[]
  accounts            Account[]
  sessions            Session[]
  searches            SearchLog[]
  auditLogs           AuditLog[]  // Track admin actions
  
  // Community sharing relations
  sharedBusinesses    SharedBusiness[]
  copiedBusinesses    BusinessCopyActivity[]
  
  createdAt           DateTime    @default(now())
  updatedAt           DateTime    @updatedAt
  
  @@index([email])
  @@index([role])
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model Cart {
  id         String     @id @default(uuid())
  name       String?
  userId     String
  businessId Int
  user       User       @relation(fields: [userId], references: [id])
  business   Business   @relation(fields: [businessId], references: [id])
  items      CartItem[]
  totalCost  Float?
  createdAt  DateTime   @default(now())
  updatedAt  DateTime   @updatedAt

  @@unique([userId, businessId])
  @@index([userId])
  @@index([businessId])
}

model CartItem {
  id              String   @id @default(uuid())
  cartId          String
  productId       Int
  quantity        Int      @default(1)
  unitPrice       Float
  category        String?  @default("Uncategorized")
  requirementName String?  @default("Unspecified Requirement")
  cart            Cart     @relation(fields: [cartId], references: [id], onDelete: Cascade)
  product         Product  @relation(fields: [productId], references: [id])
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  @@unique([cartId, productId])
  @@index([cartId])
}

model Business {
  id           Int           @id @default(autoincrement())
  slug         String        @unique
  name         String
  description  String?
  image        String?
  published    Boolean       @default(true)
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
  requirements Requirement[]
  carts        Cart[]
  searches     SearchLog[]
  user         User?         @relation(fields: [userId], references: [id])
  userId       String?
  
  // Community sharing relation
  sharedBusinesses SharedBusiness[]

  @@index([name])
  @@index([slug])
  @@index([published])
}

model Requirement {
  id          Int       @id @default(autoincrement())
  name        String
  description String?
  image       String?
  category    String?
  necessity   String    // Required, Optional
  businessId  Int
  
  business    Business  @relation(fields: [businessId], references: [id], onDelete: Cascade)
  comments    Comment[]
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  
  @@index([businessId])
  @@index([category])
}

model Comment {
  id            Int         @id @default(autoincrement())
  content       String
  requirementId Int
  userId        String
  isApproved    Boolean     @default(false) // For moderation
  requirement   Requirement @relation(fields: [requirementId], references: [id], onDelete: Cascade)
  user          User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt
  
  @@index([requirementId])
  @@index([userId])
  @@index([isApproved])
}

model Product {
  id          Int        @id @default(autoincrement())
  name        String
  description String?
  price       Float?
  image       String?
  url         String?
  vendorId    Int?
  vendor      Vendor?    @relation(fields: [vendorId], references: [id])
  cartItems   CartItem[]
  reviews     Review[]
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt
  
  @@index([vendorId])
  @@index([name])
}

model Review {
  id         String   @id @default(uuid())
  product    Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  productId  Int
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId     String
  rating     Int
  comment    String
  isApproved Boolean  @default(false) // For moderation
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  
  @@index([productId])
  @@index([userId])
  @@index([isApproved])
}

model Vendor {
  id        Int       @id @default(autoincrement())
  name      String
  website   String?
  logo      String?
  products  Product[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@unique([name, website])
  @@index([name])
}

model SearchLog {
  id          String    @id @default(uuid())
  keyword     String
  location    String?
  userId      String?
  businessId  Int?
  ipAddress   String?
  userAgent   String?
  resultCount Int?
  user        User?     @relation(fields: [userId], references: [id])
  business    Business? @relation(fields: [businessId], references: [id])
  createdAt   DateTime  @default(now())

  @@index([keyword])
  @@index([createdAt])
  @@index([userId])
}

// Audit Log for tracking admin actions
model AuditLog {
  id          String   @id @default(uuid())
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  action      String   // CREATE, UPDATE, DELETE, VIEW
  entity      String   // Business, Product, User, etc.
  entityId    String   // ID of the affected entity
  changes     Json?    // Store before/after values
  ipAddress   String?
  userAgent   String?
  createdAt   DateTime @default(now())
  
  @@index([userId])
  @@index([entity])
  @@index([action])
  @@index([createdAt])
}

// ============================================
// COMMUNITY SHARING FEATURE MODELS
// ============================================

// SharedBusiness: Tracks publicly shared business setups
model SharedBusiness {
  id          Int      @id @default(autoincrement())
  userId      String   // The user who shared the business
  businessId  Int      // The business being shared
  name        String   // Cached business name for quick access
  description String?  @db.Text // Optional description/notes about this setup
  isActive    Boolean  @default(true) // Can be toggled on/off (public/private)
  viewCount   Int      @default(0) // Track how many times previewed
  copyCount   Int      @default(0) // Track how many times copied
  
  // Featured/quality control
  isFeatured  Boolean  @default(false) // Staff picks
  featuredAt  DateTime?
  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Relations
  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  business Business @relation(fields: [businessId], references: [id], onDelete: Cascade)
  
  // Track who copied this business
  copies   BusinessCopyActivity[]

  @@unique([userId, businessId]) // One shared business per user per business type
  @@index([isActive, viewCount])
  @@index([isActive, copyCount])
  @@index([isActive, createdAt])
  @@index([isFeatured])
}

// BusinessCopyActivity: Track when users copy shared businesses (for analytics)
model BusinessCopyActivity {
  id               Int      @id @default(autoincrement())
  sharedBusinessId Int
  copiedByUserId   String
  copiedAt         DateTime @default(now())

  // Relations
  sharedBusiness SharedBusiness @relation(fields: [sharedBusinessId], references: [id], onDelete: Cascade)
  copiedByUser   User           @relation(fields: [copiedByUserId], references: [id], onDelete: Cascade)

  @@index([sharedBusinessId])
  @@index([copiedByUserId])
  @@index([copiedAt])
}
